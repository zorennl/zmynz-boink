    # right
    if is_key_down(KEY_D): 
        rx += 2
    # left
    if is_key_down(KEY_A): 
        rx -= 2
    # up
    if is_key_down(KEY_W): 
        ry -= 2
    # down
    if is_key_down(KEY_S):
        ry += 2    
